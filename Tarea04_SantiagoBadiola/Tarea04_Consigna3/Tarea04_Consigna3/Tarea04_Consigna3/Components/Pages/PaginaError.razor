@page "/PaginaError"

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Error</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background-color: white;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 90%;
        }

        h1 {
            color: #D32F2F;
            margin-bottom: 1rem;
        }

        p {
            color: #333;
            margin-bottom: 2rem;
        }

        .icon {
            font-size: 4rem;
            color: #D32F2F;
            margin-bottom: 1rem;
        }

        .button {
            background-color: #D32F2F;
            color: white;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

            .button:hover {
                background-color: #B71C1C;
            }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon">❌</div>
        <h1>Error de inicio de sesión</h1>
        <p>Lo sentimos, no se pudo iniciar sesión. Por favor, verifica tu nombre de usuario y contraseña e intenta nuevamente.</p>
    </div>
</body>
</html>